//! Thin binary wrapper. All real work lives in `vcli_daemon::run::run_foreground`.

fn main() {
    // Placeholder until Task 12 wires real argv + run_foreground().
    eprintln!("vcli-daemon: not yet wired — see Task 12");
}
